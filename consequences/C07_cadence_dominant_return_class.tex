\subsection{Consequence 7: Cadence and dominant return class}
\begin{consequence}\label{cons:c7}
The resolved local field carries a cadence spectrum and a dominant return class. Drift and reconciliation clocks organize the cadence report and the reported dominant rung $B^*$ on the same readout family.
\end{consequence}
\paragraph{Depends on.} \Cref{def:drift,def:cadence-bstar,def:cadence-extractor,def:loss-channel}.
\paragraph{Local substructure.} Cadence spectrum; drift; dominant return class; loss-rate channel.
\paragraph{Derivation sketch.} From repeated return accounting on the active-cut family together with the declared cadence extractor, phase/rotor accumulators, and dominant-return estimator. The same readout supports both drift witnesses and the return-class histogram whose dominant rung is $B^*$.
