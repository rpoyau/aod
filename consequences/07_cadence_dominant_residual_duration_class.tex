\subsection{Consequence 7: Cadence and dominant residual-duration class}
\begin{consequence}\label{cons:c7}
The resolved local field carries a cadence spectrum and a dominant residual-duration class. Drift and reconciliation clocks organize the cadence report and the reported dominant rung $B^*$ on the same readout family.
\end{consequence}
\paragraph{Depends on.} \Cref{def:drift,def:bstar-histogram,def:cadence-bstar,def:cadence-extractor,def:loss-channel}.
\paragraph{Local substructure.} Cadence spectrum; drift; dominant residual-duration class; loss-rate channel.
\paragraph{Derivation sketch.} From repeated return accounting on the declared cut family together with the declared cadence extractor, phase/rotor accumulators, and dominant residual-duration estimator. The same readout supports drift witnesses and the return-duration histogram whose dominant rung is $B^*$.
