# Authoring Report [anchor]

## Setup
Declare the stable baseline, active delta, candidate digest, active goal, scope, and frozen lanes.

## Declared choices
List every new definition, schema, transition rule, path operation, or specialization. Do not present choices as axioms.

## Derivation
For each changed claim or executable gate, list the exact AF/AFC dependencies and the changed paths. State how `apply(stable, delta)` remains exact.

## Checks
Record deterministic builds, tests, manifests, and counterfactuals with exact evidence paths and hashes.

## Falsification
List concrete mutations or observations that would make the authored result fail.

## Provenance
List exact internal anchors and imported governance rule IDs only.

## Literature note
External provenance only; never a premise.
