\subsection{C4 -- Hydrogen shell test with squared shell rendering}\label{app:c4}
\paragraph{Question.} Does the cited shell window realize the predicted shell band, dominant residual-duration class, support mass, attenuation, and loss-facing channel, and does its familiar rendering stay consistent with the squared shell convention?

\paragraph{Cited input block.} Familiar shell-facing constants are taken from the NIST 2022 CODATA recommended values, in particular the Bohr radius and the Rydberg frequency/constant tables used for the rendering layer.\footnote{NIST, \href{https://physics.nist.gov/cuu/Constants/Table/allascii.txt}{Fundamental Physical Constants --- Complete Listing}; NIST, \href{https://physics.nist.gov/cuu/pdf/wallet_2022.pdf}{2022 CODATA Recommended Values of the Fundamental Constants of Physics and Chemistry}.}

\paragraph{Declared reduction.} The row is fixed first on the cited shell ledger. Familiar hydrogen radii are then attached using the main-note squared shell-rendering convention, so the shell-facing public algebra is recorded in the form
\[
E_n^2 = \frac{v_n^2}{B_n^*(\omega_{\mathrm{H}})}
\]
rather than by equivalent square-root compression. Here $B_n^*(\omega_{\mathrm{H}})$ denotes the shell-facing support witness read on the same cited window; it is not an external constant imported from outside the row.

\paragraph{Readout.}
\begin{itemize}
\item cited shell window $\omega_{\mathrm{H}}$
\item structural key \texttt{tetron:*}
\item frame count / $T_{\mathrm{eval}}=24$ bip
\item weighting policy $w(\kappa)\equiv 1$
\item shell band $3$
\end{itemize}

\paragraph{Operator chain.}
\[
b(\kappa)\to H_B\to B^*\to \Lambda_b\to A_B\to R_{\mathrm{loss}}\to R_{\mathrm{loss}}^{(\mathrm{biz})}.
\]

\paragraph{Calculation note.} In this shell-facing section the squared public rendering is kept explicit. The support witness $B_n^*(\omega_{\mathrm{H}})$ is the shell witness carried by the cited window, and the displayed shell relation is written as a ratio before any float compression:
\[
E_n^2 = \frac{v_n^2}{B_n^*(\omega_{\mathrm{H}})}.
\]
As elsewhere in the supplement, $B^*$ is read from the burden histogram on the cited window, whereas $R_{\mathrm{loss}}$ is read from the loss/tension channel on that same window.

\paragraph{Output row.}
\begin{center}
\small
\begin{tabularx}{\linewidth}{@{}lcccccX@{}}
\toprule
Row & Shell band & $B^*$ & $\Lambda_b$ & $R_{\mathrm{loss}}$ [trit/bip] & Cadence witness & Shell rendering note \\
\midrule
C4.A & 3 & 137 & 6 & $(1+8\ell_2)/24$ & $5/12$ & squared public form $E_n^2=v_n^2/B_n^*(\omega_{\mathrm{H}})$ \\
\bottomrule
\end{tabularx}
\end{center}

\paragraph{SI rendering block.} The cited row is fixed first. Familiar hydrogen shell witnesses are then attached as
\[
f_e = 1.2355899446144601\times 10^{20}\ \text{Hz},
\]
and shell-radius witnesses
\[
r_n = n^2 a_0,\qquad
n=1:\ 5.291772191461861\times 10^{-11}\ \text{m},\quad
n=2:\ 2.1167088765847444\times 10^{-10}\ \text{m},
\]
\[
n=3:\ 4.762594972315675\times 10^{-10}\ \text{m},\quad
n=4:\ 8.466835506338978\times 10^{-10}\ \text{m}.
\]
\textit{Float policy.} The shell-facing ratio $E_n^2=v_n^2/B_n^*(\omega_{\mathrm{H}})$ is the visible public form. Displayed decimal radii and frequencies are rounded renderings only; the cited row and its ratio form remain authoritative.

\paragraph{Figure witnesses.} The shell-family schematic and colored speed graph are both generated by the companion notebook and stored once under the supplement-wide figures directory.
\begin{figure}[htbp]
\centering
\includegraphics[width=0.74\linewidth]{figures/hydrogen/hydrogen_shell_radii.png}
\caption{Hydrogen shell radii attached after the cited shell row is fixed. The plotted radii are render-layer witnesses only; the squared shell ratio remains the authoritative public form.}
\end{figure}

\paragraph{Hook / Evidence.} Use the cited shell-band and attenuation hooks together with the shell-rendering convention carried by the main note.

\paragraph{Companion notebook and manifest.} See Section~\ref{app:suppc-notebooks} for the executed calculation manifest and generated figure provenance for this section.

\begin{quote}\small\path{notebooks/hydrogen/c4_hydrogen_shell_worked.ipynb}\end{quote}
