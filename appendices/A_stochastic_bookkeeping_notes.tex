\section{Stochastic bookkeeping notes}

This appendix contains the probability-level, representation-level, and capacity-side commentary used by the same declared AOD calculus.

\subsection{Markov bookkeeping vs hidden nonlinear update}
The visible Markov description is the stochastic representation of the deeper hidden nonlinear update on the same declared distinction family, exactly as in the AFC path consequence. CK mismatch on a projected stream is read as a representation-adequacy witness on that same basis. The imported AFC Appendix~A capacity quantity $C(P)$ remains the kernel-level capacity reference. Local admissibility counts $m_t$ and the local loss readout $R_{\mathrm{loss}}$ record declared readout-side restriction on the declared cut/readout family.

\subsection{Return and backlog probabilities}
Return/backlog probabilities are admissible as declared statistics on a declared readout family. The integer rows $\beta_t$, $m_t$, and $b(\kappa)$ remain authoritative; the probabilities summarize the declared successor horizon under the current Memory State and the declared policy set.

\subsection{Narrowing adequacy under coupled readouts}
Any empirical narrowing statistic cites the coupling policy, the declared cut/readout family, and the estimator functional used. Coupled-horizon narrowing is the local AOD row carried by that same declared cut/readout family.

\subsection{Closure and crowning probabilities}
Crowning and closure probabilities may be recorded as declared race or hitting-time statistics on the same declared cut/readout family. Such probabilities certify detector adequacy and regime labels on the declared readout family.

\subsection{\texorpdfstring{$B^*$}{B*} histogram statistics and Jensen discipline}
The $B^*$ ladder uses a return-duration histogram $H_B$ with declared weights and support mass. Probability-level summaries of that histogram preserve the declared sample-set order. Histogram and attenuation operators are summarized by averaged burdens or averaged admissibility counts only under an explicit aggregation policy. Deeper statistical exploration of histogram tails, confidence bands, and rung mixtures is collected here rather than in the main body.
