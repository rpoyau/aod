\section{Stochastic bookkeeping notes}

This appendix contains the probability-level, representation-level, and capacity-side commentary used by the same declared native calculus. It introduces no new operators, no new family ontology, and no new naming rules.

\subsection{Markov bookkeeping vs hidden nonlinear update}
The visible Markov description remains the stochastic representation of the deeper hidden nonlinear update on the same declared distinction family, exactly as in the AFC path consequence. CK mismatch on a projected stream is read as a representation-adequacy witness on that same basis. The imported AFC Appendix~A capacity quantity $C(P)$ remains the kernel-level capacity reference. Local admissibility counts $m_t$ and local loss reports $R_{\mathrm{loss}}$ do not redefine $C(P)$; they report cited readout-side restriction on the declared cut/readout family.

\subsection{Return and backlog probabilities}
Return/backlog probabilities are admissible only as declared statistics on a cited readout family. They do not replace the integer ledgers $\beta_t$, $m_t$, or $b(\kappa)$, and they do not define the future; they summarize the declared successor horizon under the current Memory State and the declared policy set.

\subsection{Narrowing adequacy under coupled readouts}
Any empirical narrowing statistic must cite the coupling policy, the cited cut/readout family, and the estimator functional used. Coupled-horizon narrowing is the local AOD report carried by that same cited cut/readout family.

\subsection{Closure and crowning probabilities}
Crowning and closure probabilities may be reported as declared race or hitting-time statistics on the same cited cut/readout family. Such probabilities certify detector adequacy and regime labels on the cited readout family.

\subsection{\texorpdfstring{$B^*$}{B*} histogram statistics and Jensen discipline}
The $B^*$ ladder uses a return-duration histogram $H_B$ with declared weights and support mass. Probability-level summaries of that histogram must preserve the native sample-set order. In particular, one must not replace the histogram or attenuation operators by summaries built from averaged burdens or averaged admissibility counts unless the aggregation policy explicitly licenses that reduction. All deeper statistical exploration of histogram tails, confidence bands, and rung mixtures belongs here rather than in the main body.
