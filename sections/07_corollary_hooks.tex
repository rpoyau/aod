\section{Corollary and audit hooks}

\subsection{Corollary K1: Local AO field on cuts}
\begin{corollaryx}\label{cor:k1}
From \Cref{cons:c1,cons:c2,cons:c3,cons:c4,cons:c5,cons:c6,cons:c7,cons:c8} and the local choices, a local AO field is attached to declared cuts and their unspoolings inside the AFC/AOD calculus.
\end{corollaryx}
\paragraph{Depends on.} \Cref{cons:c1,cons:c2,cons:c3,cons:c4,cons:c5,cons:c6,cons:c7,cons:c8,def:ao-field}.
\paragraph{Derivation.} By composition of the local consequence chain and the operator layer on the same declared cut family and under the same notation/choice contract.

\subsection{Audit hooks}
\begin{hook}[H1: identity symmetry / internal-cancellation audit]
\label{hook:h1}
The declared Region/boundary rows certify the inherited AFC identity when relational symmetry and internal-cancellation balance are recovered on the cut.
\end{hook}

\begin{hook}[H2: isotropic-ignorance / anisotropic-knowledge audit]
\label{hook:h2}
An anisotropy claim is certified by an explicit local knowledge-profile declaration. An isotropic-invariance claim is certified on the subfamily where the declared refinement preserves indistinguishability.
\end{hook}

\begin{hook}[H3: topology / two-cycle / four-cycle audit]
\label{hook:h3}
A topology row is certified by the canonical Q4 chart and the two-cycle/four-cycle witness structure carried by the declared cut trace family.
\end{hook}

\begin{hook}[H4: backlog / narrowing / reconciliation audit]
\label{hook:h4}
A backlog or reconciliation claim is certified when successor narrowing, backlog, and reconciliation are reconstructed from the same declared cut family and policy set.
\end{hook}

\begin{hook}[H5: wavelet / saturation / chained-trit audit]
\label{hook:h5}
A wavelet-passage claim is certified when the AO field, wavelet passage, saturation, and loss rows carry the chained trit-carriage row on the declared Memory path.
\end{hook}

\begin{hook}[H6: push/pull / recursive phase service / torsional residual witness audit]
\label{hook:h6}
A push/pull or recursive phase-service claim is certified by the chirality-resolved boundary totals, effective-charge proxy, phase-lag witness, torsional residual witness, and positive loss channel on the same readout family.
\end{hook}

\begin{hook}[H7: cadence / dominant residual-duration class / Jensen audit]
\label{hook:h7}
A cadence or dominant residual-duration class claim is certified when the declared cadence extractor and dominant residual-duration estimator reproduce the declared cadence spectrum, $B^*$ support mass, and the required Jensen-discipline ordering on the same readout family.
\end{hook}

\begin{hook}[H8: confinement / transmutation audit]
\label{hook:h8}
A confinement or transmutation claim is certified when the declared window carries the shell predicate, shell-signature class, declared $B^*$-dominance, declared proximity rule, and the corresponding transition threshold on the same motif-conditioned row.
\end{hook}

\begin{hook}[H9: integer boundary-path / transverse-witness audit]
\label{hook:h9}
The retained boundary-transport readout is certified on a declared retained extraction by the exact integer identities
\[
r_e=0
\Rightarrow
u_e^{(\mathrm{tr})}=0
\Rightarrow
w_{\perp}=0,
\]
\[
r_e\to-r_e
\Rightarrow
\mathcal J_K\to-\mathcal J_K
\quad\text{and}\quad
w_{\perp}\to-w_{\perp},
\]
\[
\chi_e\to-\chi_e
\Rightarrow
w_{\perp}\to-w_{\perp},
\]
\[
\epsilon_{e,+}^{\perp}=-\epsilon_{e,-}^{\perp}
\Rightarrow
w_{\perp}^{+}=-w_{\perp}^{-}.
\]
These identities audit the retained cut, incidence assignment, and boundary-transport readout.
\end{hook}
