\section{Introduction}

\subsection{Aim and stance}
This note is an AFC addendum. It stays inside the declared AFC model universe and develops only the local AOD layer needed below. This note continues AFC by making explicit the temporal reading latent in the imported Stokes consequence on declared cuts.

\subsection{Upstream basis}
The note depends on AFC Stokes and the AF protocol~\cite{AFCStokes2025,AF2025}.

\subsection{Relational units and notation}
The public line uses \emph{active cut}, \emph{frame}, \emph{window}, \emph{present identity}, \emph{predecessor trace}, \emph{successor horizon}, \emph{net flux}, \emph{boundary flux}, \emph{step flux}, \emph{window current}, \emph{Memory path}, and \emph{trit}. Where a unit of update is needed, the upstream notation \emph{put} / \emph{putz} or neutral tick / rate wording is retained. The term \emph{cut} is reserved for the inherited relational localization object and is not reused as a unit alias.

\subsection{Temporal localization (AOD reading)}
On a declared readout family, the predecessor trace records the already-cut interior carried along a Memory path. The present is the active cut together with the present identity on that cut. The successor horizon is the admissible exterior continuation generated from the current Memory State, the Markov Kernel, and the declared local constraints. In this sense, temporal order is read as the emergent relational tautology of the same imported AFC identity across successive active cuts on the declared readout family.


\subsection{Native cut calculus}
This addendum proceeds from AFC Axiom~1, $N \equiv \infty \equiv \chi$, and the imported AFC Stokes identity on a declared Region $R\subseteq V$ with boundary $\partial R$. The cut localizes the same relational continuity into an interior sum and a boundary sum. A declared temporal readout then assigns ternary symbols $\tau_t\in\{-1,0,+1\}$ on cited frames and windows in the native put/putz units and notation. In this sense, the native line is a finite cut calculus. Continuum-style, cochain-style, or diagrammatic forms may be used later as renderings of the same cut bookkeeping, but they are not premises of the derivation.

\subsection{Global addendum choices}
Construction order, field/readout separation, step flux versus window current, and rate normalization are fixed globally for the addendum.

\subsection{Organization}
After the imported relational basis and the local addendum choices, the note proceeds through local consequences, the native operator layer, a short corollary-and-hooks section, examples, and two appendices.
