# AF/AFC protocol alignment audit

The active AOD note and manual follow the AF/AFC ordering:

1. inherited AFC basis;
2. local definitions and choices;
3. mathematical consequences;
4. operator/readout rows;
5. audit hooks;
6. manual-local examples and provenance.

The main note keeps the AFC/AOD calculus at the exact row layer. The manual records source-normalization maps, SI-facing rendering, benchmark comparison, reproducibility artifacts, and scaffold status.

The active provenance path is manual-local: section source, raw setup when present, script when present, derived CSV when present, figure when present, notebook when present, test when present, and audit file when present.
