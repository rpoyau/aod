% Cs-133 Fractal Elementary Bip/Monon Occurrence and M1 Execution Hardening
\section[Cs-133 Fractal Elementary Occurrence Gate]{Cs-133 Fractal Elementary Bip/Monon Occurrence and\protect\\ Canonical D.E.C. Execution Gate}
\label{app:manual1-cs133-structural-gate}

\subsection{Purpose: surface the first elementary anchor occurrence}

This gate treats caesium-133 by the same fractal-elementary discipline used for the carried 118-element application lane: declare the identity and coordinate, freeze the exact kernel family, execute the declared mode, freeze the packet, classify the support core, and only then compare with the hidden structural hypothesis. No clock frequency, observed lifetime, spectroscopy target, SI-time value, residual, or score enters the native row.

The declared identity is
\[
\mathtt{00}_{(8)}\vdash {}^{133}_{55}\mathrm{Cs},
\qquad
Z=55,
\quad
A=133,
\quad
N=A-Z=78.
\]
The frozen 118-element registry row supplies the name, symbol, and atomic-number identity card. The isotope mass number and neutron count are declared scope fields; they are not fitted targets.

At the origin anchor the native ontology records
\[
\mathtt{00}_{(8)}\vdash \mathrm{monon},\mathrm{bip},\mathrm{biz}.
\]
Accordingly, this occurrence is surfaced first at \(\mathtt{00}_{(8)}\), with \(\mathrm{bip}\) as the native count unit and monon as the primitive completion object. M1 binds this basis to a scoped occurrence; it does not certify a connected recurrence or an SI anchor.

\subsection{Identity-specific occurrence and recurring topology family}

The construction card records
\[
\begin{aligned}
\text{elementary identity}
&\longrightarrow \text{scoped occurrence at }\mathtt{00}_{(8)}\\
&\longrightarrow \text{declared }Q_4\text{ branch/hinge/branch topology family}.
\end{aligned}
\]
Identity and occurrence hashes are carried into every enumeration and execution row. The topology belongs to the recurring equivalence class
\[
\texttt{elementary\_00o8\_Q4\_branch\_hinge\_branch\_monon\_family}.
\]
Thus the occurrence is identity-specific while the topology family is not asserted to be an isotope-unique fingerprint.

A counterfactual Xe-132 packet is generated as a distinct scoped occurrence. Its identity, occurrence, kernel-enumeration packet, and execution packet hashes differ from the Cs packet. Its topology-family hash remains equal because the shared family is declared in advance. These statuses are computed from the hashes rather than inserted as conclusions.

\subsection{Kernel enumeration is not causal time}

The first nine rows are an exact support/kernel enumeration:
\begin{center}
\begin{tabular}{@{}lcccc@{}}
\toprule
stage & alternatives & weights & normalizer & probabilities\\
\midrule
left branch  & $4$ & $(1,1,1,1)$ & $4$ & $(1/4,1/4,1/4,1/4)$\\
hinge        & $1$ & $(1)$       & $1$ & $(1)$\\
right branch & $4$ & $(1,1,1,1)$ & $4$ & $(1/4,1/4,1/4,1/4)$\\
\bottomrule
\end{tabular}
\end{center}
Each row is typed
\[
\begin{gathered}
\texttt{row\_role}=\texttt{native\_structural\_kernel\_enumeration},\\
\texttt{row\_time\_semantics}=\texttt{non\_temporal\_alternative\_enumeration}.
\end{gathered}
\]
The row index is a serialization index over alternatives. It is not a bip count, an event-order relation, or a recurrence period.

The one exact kernel family is
\[
P(e\mid c,B)
=
\frac{\operatorname{adm}(e;c,B)w(e;c,B)}
{\sum_f\operatorname{adm}(f;c,B)w(f;c,B)}.
\]
Each stage has a frozen kernel ID and SHA-256. The detector verifies admissibility, positive rational weights, stored normalizer, reduced probability pair, exact textual fraction, complete alternative IDs, and exact probability sum one.

\subsection{Canonical \texttt{enumerate\_all} execution ledger}

A separate execution ledger materializes exact rational branch mass. Let the incoming source mass be one. The left kernel propagates
\[
1\longmapsto
\left(\frac14,\frac14,\frac14,\frac14\right),
\qquad
\sum_i m_i^{\mathrm{out}}=1.
\]
The four parents aggregate at the hinge:
\[
\frac14+\frac14+\frac14+\frac14=1,
\qquad
1\xrightarrow{P=1}1.
\]
The right kernel then propagates
\[
1\longmapsto
\left(\frac14,\frac14,\frac14,\frac14\right),
\qquad
\sum_i m_i^{\mathrm{out}}=1.
\]
Every execution row carries
\[
(\text{kernel ID/hash},\ \text{source state},\ \text{target state},\
\text{parent transition},\ m_{\rm in},\ P,\ m_{\rm out})
\]
with
\[
m_{\rm out}=m_{\rm in}P
\]
checked exactly. The stage audit records zero rational mass residual for left branch, hinge aggregation, and right branch.

This is a canonical structural D.E.C. execution packet. It is still not the connected primitive recurrence required in M2.

\subsection{Cryptographic freeze before classification}

The detector accepts one input only:
\[
\texttt{cs133\_pre\_detection\_freeze\_manifest.json}.
\]
The manifest locks the identity packet, scoped occurrence, construction rule, boundary card, kernel contracts, kernel enumeration, canonical execution ledger, read-only copies, stage mass audit, detector contract, and detector implementation.

Before semantic classification, the detector verifies:
\begin{itemize}[leftmargin=2em]
  \item every artifact byte count and SHA-256;
  \item every source-row and read-only-row hash;
  \item identity, occurrence, construction, coordinate, boundary, and window linkage;
  \item exact kernel IDs, normalizers, probabilities, and stage sums;
  \item exact \texttt{enumerate\_all} mass propagation and conservation;
  \item target-frequency and observation-input absence.
\end{itemize}
An undeclared mutation is rejected before classification. A deliberately generated counterfactual identity is admitted only as a separate scoped occurrence with its own packet hashes.

\subsection{Blind \texorpdfstring{$p{:}q$}{p:q} classification under declared \texorpdfstring{$L$}{L}}

The hidden card contains
\[
K_{\rm hyp}=1{:}2{:}6,
\]
but the detector may not read it. The boundary card separately declares
\[
L=|\omega_{\mathrm{Cs,struct}}|=6.
\]
Therefore the detector is blind to \(p{:}q\) and visible to the declared \(L\) scope:
\[
\boxed{\texttt{classification\_mode}=\texttt{blind\_pq\_under\_declared\_L}.}
\]

The admitted branch count is
\[
\Pi^- = \Pi^+ = 4,
\qquad
H_\mu=1.
\]
The detector solves
\[
q^p+q=4,
\qquad p\ge1,
\quad q\ge2,
\]
over the complete derived domain. Since \(q^p\ge q\),
\[
2q\le4\Longrightarrow q=2,
\]
and then
\[
2^p+2=4\Longrightarrow p=1.
\]
Thus the detected support core is
\[
\boxed{K_{\rm core}=1{:}2}.
\]
The separately declared scope gives
\[
\boxed{K_{\rm scope}=1{:}2{:}6}.
\]
The audit records, independently,
\[
\texttt{pq\_hypothesis\_match\_status}
\quad\text{and}\quad
\texttt{scope\_consistency\_status}.
\]
A hidden counterfactual \(2{:}2{:}6\) leaves the detector output unchanged, yields a \(p{:}q\) mismatch, and preserves the declared \(L\) match.

\subsection{Native support and capacity-fill audit}

The scope-conditioned row gives
\[
\Pi=4,
\qquad
H_\mu=1,
\qquad
RD_{\AO}=2\Pi+H_\mu=9.
\]
For the six-slot window,
\[
\rho^D_\omega=\min(9,6)=6.
\]
The frozen ternary-capacity rule gives
\[
C_3=3,
\qquad
P^D=C_3\rho^D_\omega=18,
\qquad
Q^D=C_3\max(0,9-6)=9.
\]
The window capacity is derived:
\[
C_{\rm window}=C_3L=3\cdot6=18.
\]
Hence the capacity-fill residual is
\[
\boxed{R_{\rm capacity}=P^D-C_{\rm window}=0},
\qquad
X_{\rm shedding}=0.
\]
This is a capacity identity under the declared clip. It is not a recurrence-closure certificate. The fields
\[
\texttt{full\_period\_closure},\quad
\texttt{proper\_prefix\_closure\_count},\quad
\texttt{primitive\_period}
\]
remain explicitly unevaluated until M2.

\subsection{Exact current claim}

\begin{quote}
\textbf{Exact M1 claim.}
The Cs-133 identity is bound to one scoped fractal-elementary occurrence at
$\mathtt{00}_{(8)}$. A hash-locked non-temporal kernel enumeration and a
canonical exact-mass D.E.C. execution packet classify the support core as
$1{:}2$ under declared $L=6$, giving the scope-conditioned form $1{:}2{:}6$.
\end{quote}
The gate does not claim an isotope-unique topology, a primitive Cs recurrence, a physical Cs clock period, or an SI-time anchor.

The simulation-freedom rule remains
\[
\boxed{
\text{native A$\Omega$D packets freeze before metrological projection or target observation.}
}
\]

\subsection{Ledger pointers and next gate}

The primary ledgers under \texttt{manual/data/cs133/} are:
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
role & ledger\\
\midrule
identity & \texttt{cs133\_identity\_packet.csv}\\
occurrence binding & \texttt{cs133\_fractal\_elementary\_occurrence\_binding.csv}\\
construction rule & \texttt{cs133\_identity\_to\_trace\_construction\_rule.csv}\\
kernel contracts & \texttt{cs133\_structural\_kernel\_contract.csv}\\
kernel enumeration & \texttt{cs133\_native\_structural\_kernel\_enumeration.csv}\\
canonical execution & \texttt{cs133\_native\_structural\_dec\_execution\_ledger.csv}\\
mass audit & \texttt{cs133\_structural\_execution\_mass\_audit.csv}\\
freeze manifest & \texttt{cs133\_pre\_detection\_freeze\_manifest.json}\\
detector/result & \texttt{cs133\_structural\_detection\_result.csv}\\
hypothesis audits & \texttt{cs133\_structural\_hypothesis\_*\_audit.csv}\\
capacity rule & \texttt{cs133\_c3\_window\_capacity\_rule.csv}\\
support audit & \texttt{cs133\_structural\_support\_audit.csv}\\
\bottomrule
\end{tabular}
\end{center}

The next gate is the connected core--outer primitive-recurrence certificate. It must construct a new state-transition system, prove full-period closure and no proper-prefix closure, conserve exact branch mass, declare the phase-transition-to-bip rule, and keep the SI caesium reference frequency absent from native recurrence inputs.

\subsection{M2 native coupled core--outer recurrence admission}

The structural classifier supplies the detected support core
\[
K_{\rm core}=p{:}q=1{:}2.
\]
The native recurrence gate does not reuse the nine M1 enumeration rows as a
period.  It declares a new connected state-transition system.  The carried
scope value $L=6$ is retained only for a post-derivation consistency check; it
is not an input to the recurrence length.

The target-free coupling card declares two native coupled states,
\[
\mathcal H_A^{\rm nat},\qquad \mathcal H_B^{\rm nat},
\]
and three support sectors derived from the detected core,
\[
N_{\rm sector}=p+q=1+2=3.
\]
Hence the state-card count is derived by the frozen rule
\[
N_{\rm phase}
=
N_{\rm coupled}\,N_{\rm sector}
=
2\cdot3
=
6.
\]
The labels $A$ and $B$ are native coupled-state labels only.  No physical
hyperfine interpretation, caesium clock frequency, observation packet, or SI
reference period enters this certificate.

The frozen coupling contract records
\begin{verbatim}
core_occurrence_id
outer_occurrence_id
core_outer_coupling_operator_id
native_coupled_state_A_id
native_coupled_state_B_id
duon_drive_contract_id
unperturbed_extraction_rule_id
execution_mode = enumerate_all
count_semantics = executed_directed_bip_transition_count
temporal_magnitude_status = not_inferred_from_trace_count
\end{verbatim}
The compatibility field formerly named \texttt{duration\_semantics} records
executed bip-transition count only.  The phase-transition rule is one declared
connected transition per bip; temporal magnitude is not inferred from that count.

\subsection{Connected six-state transition system}

The state order is
\[
\begin{aligned}
s_0&=(\text{left},A),&
 s_1&=(\text{left},B),&
 s_2&=(\text{hinge},A),\\
s_3&=(\text{hinge},B),&
 s_4&=(\text{right},A),&
 s_5&=(\text{right},B).
\end{aligned}
\]
The frozen transition relation is the single cycle
\[
\boxed{
 s_0\longrightarrow s_1\longrightarrow s_2\longrightarrow s_3
 \longrightarrow s_4\longrightarrow s_5\longrightarrow s_0.
}
\]
Every state and transition carries its identity packet, scoped occurrence,
coupling-operator, and row hashes.  The transition matrix has exactly one
incoming and one outgoing edge per state, so it is a six-cycle permutation.

The exact recurrence kernel is the same admissibility/weight family used
elsewhere:
\[
P(e\mid c,B)
=
\frac{\operatorname{adm}(e;c,B)w(e;c,B)}
{\sum_f\operatorname{adm}(f;c,B)w(f;c,B)}.
\]
Here each phase state has one admitted successor with weight one, hence
\[
\operatorname{adm}=1,
\qquad
w=1,
\qquad
Z=1,
\qquad
P=1.
\]
This is an \texttt{enumerate\_all} execution over a singleton admitted pool,
not a second normalization law.

\subsection{Exact bip-transition execution and mass audit}

Let the incoming branch mass be one.  At every phase transition,
\[
m_{k+1}=m_kP_k=1\cdot1=1,
\qquad
R^{(m)}_k=m_{k+1}-m_k=0.
\]
The exact execution ledger therefore records
\[
\sum_{k=0}^{5}R^{(m)}_k=0.
\]
Each admitted transition contributes one executed bip token:
\[
\Delta b_k=1\ \mathrm{bip}.
\]
The read-only trace freezes the ordered transitions and cumulative execution
counts
\[
1,2,3,4,5,6.
\]
These counts describe the realized path structure.  They do not by themselves
define temporal magnitude, cadence, or a reference period.

\subsection{Primitive closure certificate}

Starting at $s_0$, the state walk is
\[
(s_0,s_1,s_2,s_3,s_4,s_5,s_0).
\]
The initial state is not revisited at any proper prefix.  Thus
\[
\begin{aligned}
\texttt{full\_period\_closure}&=\texttt{passed},\\
\texttt{proper\_prefix\_closure\_count}&=0,\\
\texttt{primitive\_period\_minimality}&=\texttt{passed},\\
\texttt{primitive\_recurrence\_transition\_count}&=6,\\
\texttt{primitive\_recurrence\_bip\_count}&=6\quad\text{(compatibility alias)}.
\end{aligned}
\]
The recurrence length was derived from the connected state trace.  The equality
with the carried structural scope $L=6$ is recorded only afterward:
\[
\texttt{recurrence\_length\_uses\_declared\_L}=\texttt{false}.
\]
One completed six-transition cycle is a realized witness of the monon cycle
class for this packet.  Its six-transition count is not a monon-duration
definition; the shortest direct monon witness remains the two-transition
outbound/return path.

\subsection{Counterfactual identity and recurrence specificity}

A counterfactual xenon-132 identity packet is bound to a separate occurrence
under the same declared elementary recurrence family.  The identity,
occurrence, coupled-state, and full transition-packet hashes change.  The
abstract transition topology, primitive-cycle signature, and six-transition
execution count do not change.  The correct type is therefore
\[
\boxed{
\texttt{shared\_elementary\_recurrence\_instantiated\_at\_Cs\_scope}.
}
\]
This does not make the Cs occurrence interchangeable with the counterfactual
occurrence.  It states that the native recurrence family is shared while the
scoped packets remain identity-specific.  A later reference protocol may use
the Cs-scoped SADAR flow only after a primitive subject/reference phase lock is
frozen; the recurrence topology alone supplies no temporal reference.

\subsection{Exact M2 claim and relational temporal boundary}

\begin{quote}
\textbf{Exact M2 claim.}
Under the frozen target-free core--outer coupling contract, the Cs-scoped
fractal-elementary occurrence admits a connected six-state primitive recurrence
with exact mass conservation, no proper-prefix closure, and six admitted
executed bip transitions.  No temporal magnitude is inferred from that count.
\end{quote}

The following remain outside M2:
\begin{verbatim}
physical Cs hyperfine interpretation
9,192,631,770 Hz reference value
recurrence-to-reference correspondence
bips per AOD second
observation target
empirical residual or score
\end{verbatim}
The carried next gate reads only the certified recurrence hash and freezes a
target-blind native atom--field adapter.  In the current roadmap that adapter
is historical and unexecuted.  Any future temporal reference requires a
primitive relational lock between complete subject and sheddic-drive SADAR
flow packets.

The M2 ledgers under \texttt{manual/data/cs133/} are:
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
role & ledger\\
\midrule
coupling operator & \texttt{cs133\_core\_outer\_coupling\_operator.csv}\\
state registry & \texttt{cs133\_native\_coupled\_state\_registry.csv}\\
transition matrix & \texttt{cs133\_native\_recurrence\_transition\_matrix.csv}\\
exact execution & \texttt{cs133\_native\_recurrence\_dec\_execution\_ledger.csv}\\
read-only trace & \texttt{cs133\_native\_recurrence\_read\_only\_trace.csv}\\
mass audit & \texttt{cs133\_native\_recurrence\_mass\_audit.csv}\\
primitive certificate & \texttt{cs133\_native\_recurrence\_certificate.csv}\\
specificity audit & \texttt{cs133\_native\_recurrence\_counterfactual\_audit.csv}\\
freeze manifest & \texttt{cs133\_native\_recurrence\_pre\_certificate\_manifest.json}\\
\bottomrule
\end{tabular}
\end{center}

\subsection{M3 Cs-scoped native atom--field interaction operator freeze}

The six-bip M2 recurrence is native clockwork. It is not yet identified with
an external caesium reference transition. M3 therefore freezes a target-blind
finite-state interaction operator before any resonance search, physical clock
label, observation packet, or SI reference enters.

The admitted inputs are the M2 identity packet, scoped occurrence, coupling
operator, six-state recurrence certificate, state registry, and transition
matrix. Their hashes are locked by the input-lock ledger
\path{cs133_atom_field_input_lock.csv}. The native labels remain
\[
\texttt{native\_coupled\_state\_A},
\qquad
\texttt{native\_coupled\_state\_B}.
\]
A clock-state interpretation is only a pending mapping card. It is not an M3
input or output.

\subsubsection{Drive cadence as an exact phase class}

The drive cadence is represented by an exact phase advance
\[
\theta_d\in\mathbb{Q}/\mathbb{Z}
\quad\text{turn per bip}.
\]
Two rational values are aliases when they differ by an integer turn per bip.
The canonical representative lies in the half-open domain
\[
0\le \theta_d<1.
\]
For the six-bip primitive recurrence, the frozen M4 search grid is derived
without a target value:
\[
\mathcal V_{\mathrm{native}}
=
\left\{
\frac{k}{2N_{\mathrm{rec}}}:k=0,\ldots,2N_{\mathrm{rec}}-1
\right\},
\qquad N_{\mathrm{rec}}=6.
\]
Thus the domain contains twelve exact alias representatives. Each row stores a
reduced numerator/denominator pair; no decimal cadence is authoritative.

\subsubsection{Two-interaction window family}

M3 freezes two exact interaction windows derived from the primitive recurrence:
\[
\begin{array}{lll}
W_{1}:&w=3\ \mathrm{bip},&\text{half-cycle primary scan},\\
W_{2}:&w=5\ \mathrm{bip},&\text{pre-closure alias-resolution scan}.
\end{array}
\]
For either window the declared order is
\[
\begin{aligned}
\text{state preparation}&\longrightarrow\text{first interaction}
\longrightarrow w\text{-bip free evolution}\\
&\longrightarrow\text{second interaction}\longrightarrow\text{state readout}.
\end{aligned}
\]
The native phase advance in window $w$ is the exact rational $w/6$ turn.  The
drive phase advance is $w\theta_d$.  Their exact phase difference is
\[
\delta_w(\theta_d)
=
\left[w\theta_d-\frac{w}{6}\right]_{\mathbb{Q}/\mathbb{Z}}.
\]

\subsubsection{Exact rational finite-state response operator}

The selected arithmetic domain is
\begin{center}
\small\texttt{exact\_rational\_finite\_state\_operator}.
\end{center}
The preparation row assigns unit mass to native state $A$.  The first
interaction is the exact balanced split
\[
(M_A,M_B)=(1,0)
\longmapsto
\left(\frac12,\frac12\right).
\]
Free evolution preserves mass and advances only the frozen native and drive
phase registers.  The second interaction uses the exact phase-coincidence
response
\[
\mathcal R_w(\theta_d)
=
\begin{cases}
1,&\delta_w(\theta_d)=0,\\
0,&\delta_w(\theta_d)=\frac12,\\
\frac12,&\text{otherwise}.
\end{cases}
\]
The final packet is
\[
(M_A,M_B)
=
(1-\mathcal R_w,\mathcal R_w),
\]
so exact mass conservation is built into every response row.  This is a finite
AOD phase-coincidence operator. It is not presented as a continuum Ramsey
formula, and it makes no trigonometric approximation claim.

The response representation registry also reserves exact algebraic and exact
symbolic row types.  An algebraic row requires a minimal polynomial, root
index, and rational isolating interval.  A symbolic row requires a canonical
expression, expression hash, simplification rule, and comparison proof.  A
decimal approximation may be rendered later, but it may not select a winner.

\subsubsection{Zero-perturbation policy and controls}

The operator is frozen at the declared zero-perturbation state:
\[
\begin{aligned}
\text{external field}&=0,&
\text{thermal motion}&=0,\\
\text{collision loading}&=0,&
\text{extra boundary loading}&=0.
\end{aligned}
\]
The drive is the exact ideal balanced interaction declared by the finite-state
operator.  No post-target weak-drive extrapolation or amplitude fit is allowed.
The unperturbed candidate is the response obtained after selecting the all-zero
perturbation row before the phase scan begins.

The following control audits are frozen before execution:
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
control & required role\\
\midrule
drive off & expose a false readout or constant-response artifact\\
phase permutation & verify alias-class equivariance\\
detuned candidate & establish exact peak contrast\\
state-label permutation & test label invariance after readout remapping\\
counterfactual identity & type identity-specific packets versus shared operator family\\
\bottomrule
\end{tabular}
\end{center}
All conclusions remain \texttt{declared\_not\_executed} in M3.

\subsubsection{Exact selector, cadence, and period typing}

The resonance selector first computes exact maxima on $W_1$, then exact maxima
on $W_2$, intersects their alias classes, and returns a canonical representative
only after exact comparison.  The possible result states are
\[
\begin{gathered}
\texttt{exact\_unique\_candidate},
\quad
\texttt{exact\_tied\_candidate\_set},\\
\texttt{rational\_resonance\_interval},
\quad
\texttt{unresolved}.
\end{gathered}
\]
The primary native object is the cadence
\[
\nu_{\rm res}^{A\Omega}
=
\frac{u}{v}\ \frac{\text{turn}}{\text{bip}}.
\]
A period is materialized only for a positive unique rational cadence:
\[
B_{\rm res}^{A\Omega}
=
\frac{v}{u}\ \mathrm{bip}.
\]
Zero, tied, interval, or unresolved results do not receive an inverted period.

\subsubsection{Historical downstream reference guard}

The carried M3 adapter freezes a later reference guard.  It remains inactive
in the current roadmap.  Any downstream realization may not open unless
a later certificate supplies
\[
\begin{gathered}
\texttt{resonance\_status}=\texttt{exact\_unique\_candidate},\\
\texttt{alias\_class\_count}=1,
\quad
\texttt{zero\_perturbation\_status}=\texttt{passed},\\
\texttt{selector\_stability\_status}=\texttt{passed\_across\_frozen\_windows},\\
\texttt{peak\_contrast}>0,
\quad
\texttt{off\_resonance\_control}=\texttt{passed}.
\end{gathered}
\]
The target frequency remains absent through the native search and certificate.
A candidate set, interval, or unresolved result keeps every downstream
reference card inactive.  Even a unique phase-cadence result is not a temporal
measurement until a subject/reference SADAR lock packet is certified.

\subsubsection{Exact M3 claim and ledger pointers}

\begin{quote}
\textbf{Exact M3 claim.}
The Cs-scoped native recurrence is bound to a target-blind, hash-locked,
two-interaction exact rational phase operator.  Its cadence domain, alias rule,
interaction windows, zero-perturbation policy, response representation,
selector, controls, and later SI-activation guard are frozen before execution.
\end{quote}

M3 does not calculate a native resonance and does not identify the native
states with physical clock states.  Those operations belong to the search,
certificate, and correspondence-mode gates.

The M3 ledgers under \texttt{manual/data/cs133/} are:
\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
role & ledger\\
\midrule
input lock & \texttt{cs133\_atom\_field\_input\_lock.csv}\\
drive domain & \texttt{cs133\_drive\_phase\_domain.csv}\\
window policy & \texttt{cs133\_atom\_field\_interaction\_window\_policy.csv}\\
operator rules & \texttt{cs133\_atom\_field\_operator\_transition\_rules.csv}\\
response contract & \texttt{cs133\_atom\_field\_response\_function\_contract.csv}\\
perturbation policy & \texttt{cs133\_atom\_field\_zero\_perturbation\_policy.csv}\\
selector & \texttt{cs133\_atom\_field\_resonance\_selector\_contract.csv}\\
controls & \texttt{cs133\_atom\_field\_resonance\_control\_audit\_plan.csv}\\
SI guard & \texttt{cs133\_atom\_field\_si\_activation\_guard.csv}\\
operator declaration & \texttt{cs133\_atom\_field\_interaction\_operator.csv}\\
freeze manifest & \texttt{cs133\_atom\_field\_operator\_pre\_freeze\_manifest.json}\\
\bottomrule
\end{tabular}
\end{center}

\subsection{Roadmap reclassification: recurrence/support fixture}

The Cs-scoped atom--field operator declared in this appendix remains an exact,
hash-locked, unexecuted historical adapter.  It is not the source of native
AOD time.  The carried connected packet contains six admitted executed
transitions and is retained as a shared elementary recurrence/support fixture.
Its transition count records execution structure; it does not define a monon
duration or a universal temporal unit.

A monon remains a completed cycle class.  Its shortest direct witness contains
one outbound and one inbound bip, but that witness count is not a duration
conversion.  The \(C_6\) coordinate remains a six-slot support/enclosure
qualifier.  Temporal measurement is supplied only by a primitive relational
lock between complete SADAR flow packets, as stated in
Appendix~\ref{app:manual1-relational-temporal-semantics}.

The Cs occurrence may be revisited later as an optional downstream reference
fixture using a declared sheddic-drive flow.  No M1, M2, or M3 scientific row
is recomputed by this semantic repair, and no target frequency, observation
packet, residual, or score is introduced.
