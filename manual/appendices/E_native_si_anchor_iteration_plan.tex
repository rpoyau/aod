\section{Relational Temporal Semantics and Hydrogen-First Roadmap}
\label{app:manual1-relational-temporal-roadmap}

\subsection{Purpose of the type repair}

The certified six-transition packet at the fractal octave coordinate
\[
\mathtt{00}_{(8)}\vdash 1{:}2{:}6
\]
is retained as a shared elementary recurrence and six-slot support fixture.
It is not a universal clock conversion.  In particular, the manual does not
identify one monon with six bips and does not infer temporal magnitude from a
trace-row count.

The active temporal spine is
\[
\boxed{
\begin{gathered}
\text{scoped occurrence}
\longrightarrow
\text{D.E.C. execution}
\longrightarrow
\text{duon current}
\longrightarrow
RD/RCD
\longrightarrow
p^D
\\[-0.1em]
\longrightarrow
\text{SADAR flow}
\longrightarrow
\text{primitive subject/reference phase lock}
\longrightarrow
\text{temporal report}.
\end{gathered}}
\]
The complete relational protocol is stated in
Appendix~\ref{app:manual1-relational-temporal-semantics}.

\subsection{Bip, trace count, monon, and C6}

A \emph{bip} is one admitted executed or propagated directed beat in a
D.E.C. path.  Kernel-enumeration alternatives and CSV rows are not elapsed
bips merely because they are serialized.  A trace count records execution
structure or path resolution; it is not temporal magnitude.

A \emph{monon} is a primitive completed cycle class.  The shortest direct
witness has one outbound bip and one inbound bip around the return hinge,
\[
x\xrightarrow{\mathrm{bip}}x^\partial
 \xrightarrow{\mathrm{bip}}x,
\qquad
N_{\mathrm{bip}}^{\min}(\mathrm{monon})=2.
\]
This is a witness-count statement, not a universal duration definition.  A
blocked, delayed, recurrent, or branched realization may use a longer executed
path while remaining in the same cycle class.

The final coordinate in
\[
1{:}2{:}6,\qquad 3{:}3{:}6,\qquad 3{:}4{:}6
\]
is the six-slot support/enclosure qualifier.  It is not a monon-to-bip
conversion.  The carried six-transition recurrence remains exact execution
data, but its count is not itself a temporal reference unit.

\subsection{Hydrogen-first verification}

The first planned elementary occurrence remains
\[
\boxed{
\mathcal H_1=
\operatorname{Couple}
\bigl(3{:}3{:}6,1{:}2{:}6\bigr)
}
\]
at \(\mathtt{00}_{(8)}\), with \(Z=1\), \(A=1\), and \(N=0\).
The generator may read only frozen ontology, identity, support, D.E.C., and
relational-flow contracts.  It may not read a Balmer ratio, an observed line,
an SI unit, or a fitted time coefficient.

Four native subject/reference SADAR locks are planned for the
\(3\to2\), \(4\to2\), \(5\to2\), and \(6\to2\) transitions.  Only after
those packets freeze may the exact downstream ratio card join:
\[
\nu_\alpha:\nu_\beta:\nu_\gamma:\nu_\delta
 =500:675:756:800,
\]
\[
T_\alpha:T_\beta:T_\gamma:T_\delta
 =1512:1120:1000:945.
\]
The comparison is dimensionless and uses exact integer cross multiplication.
Observed line packets remain independent downstream objects.

\subsection{Cs, Tau, circle, and the element atlas}

The carried Cs-scoped atom--field operator is a historical, frozen, unexecuted
adapter.  It is not the source of native AOD time.  A future Cs reference card
must first freeze a relational lock between the Cs SADAR flow and a declared
sheddic-drive SADAR flow.  Physical metrological labels attach only after the
native lock packet is frozen.

Tau is not treated as a periodic clock.  Its future temporal lane is an exact
survival/hazard process relative to a reference SADAR flow.  No fixed Tau bip
count is converted to seconds.

The circle lane remains dimensionless.  Independent circumference and area
traces produce the exact report residual
\[
R_{CA}=C_\zeta r_\zeta-2A_\zeta.
\]
Zero is audited, not imposed.  The circle lane does not require a temporal
trace-count identity.

All 118 element transitions use the same relational protocol.  Each row names
a subject flow, a drive or reference flow, a primitive phase-lock packet, and
an optional report card.  No element receives its own time conversion.

\subsection{Versionless milestone sequence}

\begin{longtable}{@{}p{0.09\linewidth}p{0.34\linewidth}p{0.49\linewidth}@{}}
\toprule
ID & Milestone & Exit condition \\
\midrule
\endfirsthead
\toprule
ID & Milestone & Exit condition \\
\midrule
\endhead
R0 & Relational temporal semantics repair & The monon/bip type error is removed; SADAR phase-lock and sheddic exchange schemas are frozen; no scientific row is recomputed. \\
R1 & Consequent-form compatibility and flow admission & The \(3{:}3{:}6\) and \(3{:}4{:}6\) occurrences reference the same support packet and declare their own D.E.C./SADAR flow inputs. \\
R2 & H-1 native occurrence & The target-blind hydrogen occurrence freezes. \\
R3 & Hydrogen transition and SADAR-lock atlas & Native transition flows and primitive relational locks materialize without target values. \\
R4 & Exact Balmer relational-ratio audit & Native packets precede the exact ratio card; the audit uses integer cross multiplication. \\
R5 & Hydrogen isotope extension & H-2 and H-3 use the same relational protocol under declared extension packets. \\
R6 & 118-element relational transition atlas & Each transition carries subject, drive, and phase-lock packet IDs; no private temporal coefficient is admitted. \\
R7 & Hyperfine C6 compatibility/divisibility audit & Compatibility is tested after native flow freeze and may mismatch or remain unresolved. \\
R8 & Native rest-cycle relational atlas & Rest-cycle and transition-flow packets remain distinct. \\
R9 & Optional metrological reference cards & Exact external report coordinates attach after native relational locks and cannot alter them. \\
R10 & Circle relational geometry audit & Independent traces produce \(R_{CA}\) and refinement rows; zero is not assumed. \\
R11 & Tau survival/hazard and migration audit & Tau uses stochastic survival relative to a reference flow; all changes receive typed migration reasons. \\
R12 & Manual-I consolidation & Protocol completeness, not empirical agreement, controls exit. \\
R13 & Manual-II reassessment & The frozen application manual is reassessed in a synchronized later release. \\
\bottomrule
\end{longtable}

\subsection{Governance}

During R0--R12 the main note, shared protocol source, and Manual II remain
frozen.  Manual-I-specific appendices, data, scripts, tests, roadmaps, and
release metadata may change.  Empirical mismatch is a typed scientific result
and does not block protocol consolidation.
