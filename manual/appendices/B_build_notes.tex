% Traceability: manual-local build and release notes appendix
\section{Build notes}

\subsection{Authority and build policy}
The root \path{refs.bib} file is authoritative for the document family; the manual copy is a managed mirror used for local compilation. The public release set consists of \path{main.pdf} and \path{manual.pdf}, with optional source and audit archives.

\subsection{Table source policy}
Visible manual tables are authoritative. Curated CSV files feed numeric report-side tables where declared; notebook outputs remain companion materials only.

\subsection{Notebook link policy}
Appendix~A records notebook links only. Those notebooks support inspection and recomputation, but they do not supersede the visible rows in the manual.

\subsection{Source-only traceability and asset policy}
Detailed traceability, CSV inventories, figure inventories, and internal artifact routing remain source-only. See \path{manual/traceability-map.md} and \path{manual/artifact-map.md}.

\subsection{Current migration note}
The present source tree still retains supplement-era companion notebook paths until manual-local notebooks are created. This does not change the authority rule: the main note remains the theorem source and the manual remains the downstream public document.
