# Supplement B artifacts

These CSV files support the downstream renderings in Supplement B.

Included:
- E10 recurrent ring / recurrent-network rendering
- E11D downstream decimal / decimal rendering
