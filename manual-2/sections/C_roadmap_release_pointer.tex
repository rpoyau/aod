\section{Roadmap and release-note pointer}

Roadmap and package-status details live outside the main worked-example flow. The source file
\[
\texttt{MANUAL\_II\_ROADMAP.md}
\]
records the current milestone and the next declared gate. The release-readiness and patch-summary files record packaging status and checksums.

The operational rule for future additions is unchanged: declare a row, compute it, freeze it, map it to a target only after the freeze, and keep the ledger row auditable.
