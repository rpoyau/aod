\section{Molecular Matter Transition Atlas Materialization}

This Manual-II lane materializes a target-blind Molecular Matter transition atlas over the existing molecular fixture packets. The lane is native AOD bookkeeping only: it records molecular occurrence cards, native transition packet declarations, and SADAR-flow declarations without reading target values or attaching metric reports.

\subsection{Materialized rows}

The atlas contains the following deterministic rows:
\begin{center}
\begin{tabular}{lr}
\toprule
Ledger & Row count \\
\midrule
Occurrence cards & 23 \\
Transition packets & 23 \\
SADAR-flow declarations & 23 \\
Target joins & 0 \\
SI reports & 0 \\
Metric reports & 0 \\
Residuals & 0 \\
Scores & 0 \\
\bottomrule
\end{tabular}
\end{center}

The occurrence cards are derived from the carried molecular component fixtures and frozen chain-formula packets. The first group contains all carried molecular component fixtures. The second group contains the peptide, nucleotide, and dinucleotide chain fixtures already carried by the Molecular Matter lane.

\subsection{Closed-lane discipline}

Every Molecular Matter transition packet keeps the following statuses:
\begin{itemize}[leftmargin=1.4em]
\item \texttt{target\_value\_read\_status = not\_read};
\item \texttt{si\_report\_status = closed};
\item \texttt{metric\_report\_status = closed};
\item \texttt{subject\_reference\_phase\_lock\_status = closed};
\item \texttt{residual\_status = not\_computed};
\item \texttt{score\_status = no\_score}.
\end{itemize}

Thus the molecular rows are native transition declarations, not external compound comparisons and not empirical molecular predictions.

\subsection{Counterfactual audit}

The fail-closed audit rejects four mutations: target values inserted into molecular packets, SI frequency promoted to a native premise, metric report values materialized in the native lane, and residual scores inserted before an authorized target join. The unchanged control row passes.

\aodprovenance{Generated by \texttt{manual-2/scripts/build\_molecular\_matter\_transition\_atlas.py}. The manifest is \texttt{manual-2/data/molecular/molecular\_matter\_transition\_atlas\_manifest.json}.}
