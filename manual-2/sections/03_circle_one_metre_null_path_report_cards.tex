\section{Circle One-Metre Null-Path Report Cards}

\paragraph{Scope.}
This section materializes downstream one-metre null-path report-card records for the circle audit.  The records are report-card contracts only.  They do not alter the native symbolic circle traces, do not instantiate a metric radius inside a native packet, and do not read an empirical target.

\[
\boxed{\text{one-metre/null-path card}=\text{downstream report contract, not native premise}}
\]

\paragraph{Null-path reference card.}
The report-card relation is the declared null/light-path relation
\[
d=c\,t.
\]
The exact constant entry records
\[
c=299792458\;\mathrm{m\,s^{-1}}
\]
only as report-card data.  The one-metre report symbol is stored as a downstream radius report card, and the light-time relation is recorded exactly as
\[
t_{1\mathrm{m}}=\frac{1}{299792458}\;\mathrm{s}.
\]
This row is not a native trace input and is not a target observation.

\paragraph{Link to the finite circle row.}
The finite circle audit continues to use the symbolic radius $r$ and the frozen finite trace row.  The new link index records that a one-metre report card may be attached only after the finite trace row is frozen:
\[
\text{native trace}\rightarrow\text{finite symbolic audit}\rightarrow\text{optional metric report card}.
\]
The metric binding remains declared but not instantiated.

\paragraph{Closed lanes.}
\[
\boxed{
\begin{array}{c}
\mathrm{native\ premise}=\mathrm{forbidden},\quad
\mathrm{metric\ report}=\mathrm{contract\ declared\ not\ instantiated},\\
\mathrm{target\ read}=\mathrm{not\ read},
\quad \mathrm{residual}=\mathrm{not\ computed},
\quad \mathrm{score}=\mathrm{no\ score}
\end{array}}
\]

\paragraph{Counterfactual audit.}
The fail-closed rows reject one metre as a native radius input, the speed of light as a D.E.C. kernel parameter, a null-path report-card link before trace freeze, and a target-radius observation read in this gate.  The unchanged report-card control passes.

\paragraph{Materialized files.}
\begin{center}
\small
\begin{tabular}{ll}
\toprule
File & Role\\
\midrule
\texttt{circle\_one\_metre\_null\_path\_report\_cards.csv} & Null-path report-card contract\\
\texttt{circle\_one\_metre\_report\_link\_index.csv} & Link from symbolic circle audit to report card\\
\texttt{circle\_one\_metre\_null\_path\_counterfactual\_audit.csv} & Fail-closed report-card checks\\
\texttt{circle\_one\_metre\_null\_path\_report\_cards\_manifest.json} & Hash manifest\\
\bottomrule
\end{tabular}
\end{center}
