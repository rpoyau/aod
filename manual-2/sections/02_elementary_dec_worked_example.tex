\section{Elementary D.E.C. worked row}

This worked row uses the existing elementary $3{:}3{:}6$ fixture. It is a retained-capacity registry-coordinate example, not a new nuclear-synthesis claim. The point is to substitute fusion-scale content into the D.E.C. row form:
\[
B\to\text{D.E.C. row pair}\to\text{kernel}\to\text{trace}\to\text{A\(\Omega\) motif / SADAR}\to\text{frozen row}\to\text{target map}.
\]

\subsection{Declare the boundary package}

Declare
\[
B=B_{336}=(\partial_TF_{336},\omega_{336}).
\]
The boundary package has two admitted retained edges,
\[
e_L:v_0\to v_1,
\qquad
 e_R:v_1\to v_2,
\]
each with weight $3$.

\subsection{Write the raw D.E.C. rows}

The raw AFC edge execution is the D.E.C. row pair:
\begin{center}
\scriptsize
\begin{tabularx}{0.99\linewidth}{@{}llllllllll@{}}
\toprule
\texttt{tick} & \texttt{B} & \texttt{v} & \texttt{e} & \texttt{v\_e} & $\sigma(e)$ & $\operatorname{adm}(e;B)$ & $w(e;B)$ & $P(e;B)$ & $\operatorname{route}(e;B)$ \\
\midrule
0 & $B_{336}$ & $v_0$ & $e_L$ & $v_1$ & $+1$ & 1 & 3 & $1/2$ & retained \\
1 & $B_{336}$ & $v_1$ & $e_R$ & $v_2$ & $+1$ & 1 & 3 & $1/2$ & retained \\
\bottomrule
\end{tabularx}
\end{center}
Element names, PubChem rows, and registry targets are absent from the raw row.

\subsection{Normalize the exact kernel}

The normalizer is
\[
Z_B=\sum_{e'}\operatorname{adm}(e';B_{336})w(e';B_{336})=3+3=6.
\]
Thus
\[
P(e_L;B_{336})=\frac{1\cdot3}{6}=\frac12,
\qquad
P(e_R;B_{336})=\frac{1\cdot3}{6}=\frac12.
\]

\subsection{Trace, A\(\Omega\) motif, and SADAR context}

The read-only trace records the retained edge weights:
\[
T_{336}=(3,3).
\]
A$\Omega$D now names the detected retained-capacity motif, read as the curling-curls specification for this elementary fixture:
\[
\mathrm{motif}_{336}=\texttt{retained\_capacity\_pair}.
\]
The SADAR context records the downstream detector context:
\[
\mathrm{sadar}_{336}=\texttt{retained\_capacity\_reclosure\_context}.
\]
The retained-capacity detector gives
\[
D_{\mathrm{cap}}(T_{336})=3+3=6.
\]

\subsection{Freeze before target lookup}

The frozen AOD packet is
\[
X_{336}=\texttt{retained\_capacity}=6.
\]
This is the exact row. It is still only an AOD retained-capacity packet.

\subsection{Map to the elementary registry only after freeze}

Only after $X_{336}$ is frozen, apply the registry map:
\[
\Pi^{E0}_{\mathrm{registry}}(6)=(Z=6,\ \mathrm{Symbol}=C,\ \mathrm{Name}=\mathrm{Carbon}).
\]
The PubChem/element lane is a downstream lookup and comparison coordinate, not a raw D.E.C. premise.

\subsection{Residual and ledger}

The signed residual is
\[
\Delta Z=6-6=0,
\qquad
\delta_3=(0,0).
\]

\aodnoteblock{Ledger pointer}{The retained-capacity row is recorded in \path{manual-2/data/elementary/fusion_ladder_336.csv}. The downstream lookup lane is recorded in \path{manual-2/data/elementary/pubchem_element_map.csv}.}

\aodnoteblock{Teaching point}{The D.E.C. row form is preserved. This application substitutes the fusion-scale boundary package $B_{336}$ and delays the element-registry map until the retained-capacity packet has frozen.}
