# Alpha-Omega Dynamics: The Hidden Temporal Dynamics of Stokes

**Version:** v39.99r71

## Title and subtitle

**Title:** Alpha-Omega Dynamics

**Subtitle:** The Hidden Temporal Dynamics of Stokes

**Zenodo title:** Alpha-Omega Dynamics: The Hidden Temporal Dynamics of Stokes

## Description

Alpha↔Omega Dynamics (AΩD) is a relational temporal form of the Stokes cut of the Axiomatic–Fundamentalism calculus (AFC). The starting point is Null potential, declared distinction, relation, region, boundary, and the Stokes accounting induced by a declared cut.

AΩD continues from the AFC Stokes cut by uncovering its hidden relational temporal motifs. This is the Art of the Leprechaun: tracing the cut closely enough to learn how a larger relational system may be set up as a declared finite scale, with its boundary, window, and accounting choices made explicit. The core note gives the calculus; the manual carries the downstream finite simulations, exact fixtures, diagnostics, observable maps, external comparisons, and setup-error audits.

The main note develops the AFC/AΩD ontology and calculus of retained Stokes-wave forms: cut-running, branch/hinge/branch return, curling-curl recurrence, fractal tesseract support, tesseract-admissible boundaries, reflection duration, reflection-duration coupling, window-clipped duration participation, pressure, SADAR boundary accounting, closure residuals, sheddic routes, Fractal Field Ring Gate recurrence, and field dynamics.

The manual provides the practical simulation layer. It begins with pen-and-paper AFC raw execution through the D.E.C. ledger, then proceeds to exact internal fixtures, field rest-energy entries, shell tests, boundary-current applications, solar observable-map records, field-dynamics applications, galactic lensing diagnostics, data-support classes, scoped-error accounting, and prediction-test fixture registries. The manual separates exact integer fixtures from external comparison coordinates and treats residuals only inside declared data-support classes.

This release includes the main note, manual, source package, test output, patch summary, bundle, and SHA-256 manifests.

## Version

See `CANONICAL_VERSION.txt`.

## Build and release

Use `scripts/build_release_bundle.py` to generate versioned release artifacts from the version-neutral source tree. The release version is read from `CANONICAL_VERSION.txt` or passed explicitly to the build script. Versioned names are generated only as release artifacts; source-internal paths remain version-neutral. The canonical release artifact prefix is `AOD_Temporal_Dynamics_v39_99r71`.

## Release checks

The source package includes the test suite under `tests/`. Release bundles include the test output and SHA-256 manifests for artifact verification.
