Committed inputs and later response

Main eq:causal-update consumes S^W and S^cross prefixes committed through k and realizes S^W at k+1. Identity, closure memory, present coordinates, cadence, pressure, boundary/capacity and retained rebalancing coordinate remain separate. Attention is an input; motion is the realized state transition.

The exact rational example is J_next=J+A/p for declared p>0. Neither this model nor its nonlinearity in pressure is asserted universally. Its current, pressure and attention operands reject binary floats. FRG preserves distinction, retained relation, admitted motif, then model order.

Contact comparison requires two distinct recurrent lineages, two distinct committed contacts with separate cross-return certificates, compatible scope/scale, and committed recurrence prefixes. Directional RCD is reconstructed from the bound support and reflection counts. The prior lag scalar and bias arguments use the original a|b orientation; reversing the output negates both signed offsets and recomputes RCD.

Motif/fold witnesses in this executable realization preassemble component closures before the target starts. The general Main condition is commit-before-consumption; the stronger fixture ordering is not a new universal axiom. Outer certification binds exact inner IDs, certificates and scope map; one outer closure is one phase, and fresh outer returns supply recurrence. Exposure preserves surviving inner history during outer shedding.

M1 exposes a retained-context state coordinate but does not claim active causal awareness. M2 must extend and intervene on that coordinate only after the complete M1 gate.
